import scipy.io as sio
import matplotlib.pyplot as  plt

# Read auroral ionization rate data
d=sio.loadmat("A_ionization4JV20240112.mat")

eb=200
# production
for i in range(a["Ie"].shape[1]):
    q=a["dE"][0,eb]*a["Ie"][eb,i]*a["A"][:,eb]
    plt.plot(q)
plt.show()
